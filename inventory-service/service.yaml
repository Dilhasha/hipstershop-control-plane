apiVersion: v1
kind: Service
metadata:
  name: inventoryservice-svc
spec:
  selector:
    app: inventoryservice
  ports:
    - protocol: TCP
      port: 9233
      targetPort: 9233
  type: LoadBalancer
