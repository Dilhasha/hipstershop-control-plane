apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - microrts-route-svc.yaml

patchesStrategicMerge:
  - microrts-api-ingress.yaml

namespace: ingress-nginx

# Add any patches or custom configurations here
